

// const manager = NativeModules.AppManager;
// const emitter = new NativeEventEmitter(manager);

// emitter.addListener('error', console.error);
// emitter.addListener('download', ({ name }) => {
//   manager.runApp(name);
// });

const manager = {};

export default manager;
